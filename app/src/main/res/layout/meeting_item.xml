<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="120dp"
    android:layout_height="match_parent">

    <TextView
        android:textColor="@color/meeting_user_name_text"
        android:text="hello world"
        android:textSize="18sp"
        android:layout_above="@+id/meeting_item_user_image"
        android:layout_marginBottom="20dp"
        android:layout_centerHorizontal="true"
        android:id="@+id/meeting_item_user_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <!--<ImageView-->
        <!--android:layout_marginLeft="2dp"-->
        <!--android:id="@+id/meeting_item_tag_image"-->
        <!--android:layout_alignTop="@+id/meeting_item_user_name"-->
        <!--android:layout_toRightOf="@+id/meeting_item_user_name"-->
        <!--android:src="@drawable/tag_normal"-->
        <!--android:layout_width="wrap_content"-->
        <!--android:layout_height="wrap_content" />-->

    <ImageView
        android:scaleType="centerInside"
        android:id="@+id/ch_image"
        android:src="@drawable/meeting_item_bg_one"
        android:layout_centerInParent="true"
        android:layout_width="80dp"
        android:layout_height="80dp" />

    <org.sssta.sevenor.ui.CircleImageView
        android:layout_alignRight="@+id/ch_image"
        android:layout_alignBottom="@+id/ch_image"
        android:id="@+id/meeting_item_user_image"
        android:src="@drawable/user_image"
        android:layout_marginBottom="58dp"
        android:layout_width="70dp"
        android:layout_height="70dp" />


</RelativeLayout>